# Tapetide Financial Data Downloader
#
# This project intentionally uses ONLY the Python standard library
# (Python 3.9+). There are no runtime dependencies — nothing to install:
#
#     pip install -r requirements.txt   # (no-op, kept for tooling compat)
#
# This file exists because some tools and hosting platforms expect it.

# Runtime dependencies: none
