# NOTE: the downloaded dataset under out/ is intentionally tracked in git
# (45 MB, Nifty 500 JSON + CSVs) so it can be downloaded from GitHub.
# To un-track it again, run: git rm -r --cached out && git commit -m "untrack data"
*.json.bak

# Runtime logs
download.log
errors.log

# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/

# Editors / OS
.vscode/
.idea/
.DS_Store
Thumbs.db

# Never commit a real MCP token — only mcp_config.example.json is tracked
mcp_config.json
